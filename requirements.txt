# Dipendenze runtime pinnate per build riproducibili.
# Aggiornare con: pip install -U <pkg> && pip freeze | grep -i <pkg>

# --- Core / modelli di dominio ---
pydantic==2.13.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
loguru==0.7.3
PyYAML==6.0.3

# --- Analisi statica (adapters) ---
checkov==3.2.459
semgrep==1.52.0
tree-sitter==0.25.2
tree-sitter-python==0.25.0
tree-sitter-javascript==0.25.0

# --- Analisi dinamica / cloud ---
zaproxy==0.5.0
boto3==1.43.24
S3Scanner==2.0.2
requests==2.34.2
httpx==0.28.1

# --- Autenticazione / crypto ---
PyJWT==2.13.0
cryptography==48.0.0

# --- Presentazione (REST API + dashboard web) ---
fastapi==0.136.3
uvicorn==0.49.0
Jinja2==3.1.6

# --- Test ---
pytest==9.1.1
pytest-asyncio==1.4.0
